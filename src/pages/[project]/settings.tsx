import { Card, CardHeader, CardTitle } from '@/components/ui/Card';

export default function Settings() {
  return (
    <Card className="w-full h-full m-4">
      <CardHeader>
        <CardTitle className="text-xl">设置</CardTitle>
      </CardHeader>
    </Card>
  );
}
